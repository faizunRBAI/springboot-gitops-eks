# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Compile and verify formatting
      run: mvn -B -ntp clean compile
      working-directory: app
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Run unit tests
      run: mvn -B -ntp test
      working-directory: app
    - name: Publish test summary
      run: 'echo "### Test results" >> "$GITHUB_STEP_SUMMARY"

        find app/target/surefire-reports -name ''*.txt'' -exec cat {} + >> "$GITHUB_STEP_SUMMARY"
        || true

        '
  provision:
    runs-on: ubuntu-latest
    needs:
    - test
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 45
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Terraform validate
      run: terraform validate
      working-directory: infra
    - name: Terraform apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
  build_push:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - id: tf
      name: Read the ECR repository URL from terraform state
      run: "set -euo pipefail\nterraform init -input=false -reconfigure \\\n  -backend-config=\"\
        bucket=${{ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"ecr_url=$(terraform output -raw ecr_repository_url)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Package application
      run: mvn -B -ntp clean package -DskipTests
      working-directory: app
    - id: meta
      name: Derive image tag
      run: echo "image_tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"
    - name: Build image
      run: docker build -t local/app:${{ steps.meta.outputs.image_tag }} .
      working-directory: app
    - name: Show accepted vulnerability exceptions
      run: 'echo "### Accepted vulnerability exceptions" >> "$GITHUB_STEP_SUMMARY"

        echo ''```'' >> "$GITHUB_STEP_SUMMARY"

        grep -v ''^\s*#'' app/.trivyignore | grep -v ''^\s*$'' >> "$GITHUB_STEP_SUMMARY"
        || true

        echo ''```'' >> "$GITHUB_STEP_SUMMARY"

        cat app/.trivyignore

        '
    - name: Scan image with Trivy
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: local/app:${{ steps.meta.outputs.image_tag }}
        format: table
        exit-code: '1'
        severity: HIGH,CRITICAL
        ignore-unfixed: 'true'
        trivyignores: app/.trivyignore
    - name: Confirm the AWS identity in use
      run: 'set -euo pipefail

        aws sts get-caller-identity

        '
    - id: ecr
      name: Log in to ECR
      uses: aws-actions/amazon-ecr-login@v2
    - name: Tag and push image to ECR
      run: 'set -euo pipefail

        TAG="${{ steps.meta.outputs.image_tag }}"

        docker tag "local/app:${TAG}" "${ECR_URL}:${TAG}"

        docker push "${ECR_URL}:${TAG}"

        echo "Pushed ${TAG} to ECR."

        '
      env:
        ECR_URL: ${{ steps.tf.outputs.ecr_url }}
    - name: Record the image reference for the GitOps values
      run: "set -euo pipefail\nTAG=\"${{ steps.meta.outputs.image_tag }}\"\n{\n  echo\
        \ \"### Image published\"\n  echo \"\"\n  echo \"If gitops/application-values.yaml\
        \ does not already point at\"\n  echo \"this tag, update it and commit through\
        \ the platform:\"\n  echo \"\"\n  echo '```yaml'\n  echo \"image:\"\n  echo\
        \ \"  repository: ${ECR_URL}\"\n  echo \"  tag: ${TAG}\"\n  echo '```'\n}\
        \ >> \"$GITHUB_STEP_SUMMARY\"\n"
      env:
        ECR_URL: ${{ steps.tf.outputs.ecr_url }}
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    - build_push
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.33.0
    - name: Set up Helm
      uses: azure/setup-helm@v4
      with:
        version: v3.16.3
    - id: tf
      name: Read infrastructure outputs from terraform state
      run: "set -euo pipefail\nterraform init -input=false -reconfigure \\\n  -backend-config=\"\
        bucket=${{ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"cluster_name=$(terraform output -raw cluster_name)\" >> \"$GITHUB_OUTPUT\"\
        \necho \"lbc_role_arn=$(terraform output -raw lbc_role_arn)\" >> \"$GITHUB_OUTPUT\"\
        \necho \"vpc_id=$(terraform output -raw vpc_id)\" >> \"$GITHUB_OUTPUT\"\n"
      working-directory: infra
    - name: Update kubeconfig
      run: aws eks update-kubeconfig --name "${{ steps.tf.outputs.cluster_name }}"
        --region us-east-1
    - name: Install AWS Load Balancer Controller
      run: "set -euo pipefail\nhelm repo add eks https://aws.github.io/eks-charts\n\
        helm repo update\nkubectl create serviceaccount aws-load-balancer-controller\
        \ -n kube-system \\\n  --dry-run=client -o yaml | kubectl apply -f -\nkubectl\
        \ annotate serviceaccount aws-load-balancer-controller -n kube-system \\\n\
        \  eks.amazonaws.com/role-arn=\"${{ steps.tf.outputs.lbc_role_arn }}\" --overwrite\n\
        helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller\
        \ \\\n  --version 1.10.1 --namespace kube-system \\\n  --set clusterName=\"\
        ${{ steps.tf.outputs.cluster_name }}\" \\\n  --set region=us-east-1 \\\n \
        \ --set vpcId=\"${{ steps.tf.outputs.vpc_id }}\" \\\n  --set serviceAccount.create=false\
        \ \\\n  --set serviceAccount.name=aws-load-balancer-controller\nkubectl -n\
        \ kube-system rollout status deployment/aws-load-balancer-controller --timeout=8m\n"
      timeout-minutes: 12
    - name: Install Argo Rollouts controller
      run: "set -euo pipefail\nhelm repo add argo https://argoproj.github.io/argo-helm\n\
        helm repo update\nhelm upgrade --install argo-rollouts argo/argo-rollouts\
        \ \\\n  --version 2.38.2 --namespace argo-rollouts --create-namespace\nkubectl\
        \ -n argo-rollouts rollout status deployment/argo-rollouts --timeout=8m\n"
      timeout-minutes: 12
    - name: Install kube-prometheus-stack
      run: "set -euo pipefail\nhelm repo add prometheus-community https://prometheus-community.github.io/helm-charts\n\
        helm repo update\nhelm upgrade --install monitoring prometheus-community/kube-prometheus-stack\
        \ \\\n  --version 66.3.1 --namespace monitoring --create-namespace \\\n  --values\
        \ gitops/values/monitoring-values.yaml \\\n  --set grafana.adminPassword=\"\
        ${{ secrets.GRAFANA_ADMIN_PASSWORD }}\"\nkubectl -n monitoring rollout status\
        \ statefulset/prometheus-monitoring-kube-prometheus-prometheus --timeout=15m\n"
      timeout-minutes: 20
    - name: Install ArgoCD
      run: "set -euo pipefail\nHASH=$(printf '%s' \"${ADMIN_SECRET}\" | htpasswd -niBC\
        \ 10 admin | cut -d: -f2)\nif [ -z \"${HASH}\" ]; then\n  echo \"ERROR: could\
        \ not derive the ArgoCD admin credential hash.\"\n  exit 1\nfi\nhelm upgrade\
        \ --install argocd argo/argo-cd \\\n  --version 7.7.11 --namespace argocd\
        \ --create-namespace \\\n  --values gitops/values/argocd-values.yaml \\\n\
        \  --set-string configs.secret.argocdServerAdminPassword=\"${HASH}\"\nkubectl\
        \ -n argocd rollout status deployment/argocd-server --timeout=10m\nkubectl\
        \ -n argocd rollout status statefulset/argocd-application-controller --timeout=10m\n"
      timeout-minutes: 15
      env:
        ADMIN_SECRET: ${{ secrets.ARGOCD_ADMIN_PASSWORD }}
    - name: Remove the superseded bootstrap admin secret
      run: 'kubectl -n argocd delete secret argocd-initial-admin-secret --ignore-not-found=true
        || true

        '
    - name: Report platform state
      run: 'helm list --all-namespaces || true

        echo "--- pods ---"

        kubectl get pods -A || true

        '
  gitops_handoff:
    runs-on: ubuntu-latest
    needs:
    - configure
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.33.0
    - id: tf
      name: Read the cluster name from terraform state
      run: "set -euo pipefail\nterraform init -input=false -reconfigure \\\n  -backend-config=\"\
        bucket=${{ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"cluster_name=$(terraform output -raw cluster_name)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Update kubeconfig
      run: aws eks update-kubeconfig --name "${{ steps.tf.outputs.cluster_name }}"
        --region us-east-1
    - name: Verify the GitOps values are real
      run: "set -euo pipefail\necho \"--- gitops/application-values.yaml (image block)\
        \ ---\"\nsed -n '/^image:/,/^replicaCount/p' gitops/application-values.yaml\n\
        if grep -q 'PLACEHOLDER' gitops/application-values.yaml; then\n  echo \"\"\
        \n  echo \"ERROR: gitops/application-values.yaml still contains a PLACEHOLDER.\"\
        \n  echo \"ArgoCD cannot render the chart from placeholder values.\"\n  echo\
        \ \"Update image.repository and image.tag in that file, commit it,\"\n  echo\
        \ \"and run the deploy again. The build_push job summary prints the\"\n  echo\
        \ \"exact values to use.\"\n  exit 1\nfi\necho \"GitOps values are concrete.\"\
        \n"
    - name: Apply the ArgoCD root application
      run: "set -euo pipefail\nsed \"s|__REPO_URL__|https://github.com/${{ github.repository\
        \ }}.git|g\" \\\n  gitops/root-app.yaml | kubectl apply -f -\n"
    - name: Confirm the application exists
      run: "kubectl -n argocd get applications || true\n{\n  echo \"### GitOps handoff\
        \ complete\"\n  echo \"\"\n  echo \"ArgoCD now owns the application. CI does\
        \ not deploy.\"\n} >> \"$GITHUB_STEP_SUMMARY\"\n"
  verify:
    runs-on: ubuntu-latest
    needs:
    - provision
    - configure
    - gitops_handoff
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.33.0
    - id: tf
      name: Read cluster name from terraform state
      run: "set -euo pipefail\nterraform init -input=false -reconfigure \\\n  -backend-config=\"\
        bucket=${{ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"cluster_name=$(terraform output -raw cluster_name)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Update kubeconfig
      run: aws eks update-kubeconfig --name "${{ steps.tf.outputs.cluster_name }}"
        --region us-east-1
    - name: Show cluster state
      run: 'kubectl get nodes -o wide || true

        echo "--- platform pods ---"

        kubectl get pods -n kube-system || true

        kubectl get pods -n argocd || true

        kubectl get pods -n argo-rollouts || true

        kubectl get pods -n monitoring || true

        '
    - name: Wait for ArgoCD to reconcile the application
      run: "set -euo pipefail\nfor i in $(seq 1 60); do\n  SYNC=$(kubectl -n argocd\
        \ get application springboot-app -o jsonpath='{.status.sync.status}' 2>/dev/null\
        \ || echo Unknown)\n  HEALTH=$(kubectl -n argocd get application springboot-app\
        \ -o jsonpath='{.status.health.status}' 2>/dev/null || echo Unknown)\n  PHASE=$(kubectl\
        \ -n argocd get application springboot-app -o jsonpath='{.status.operationState.phase}'\
        \ 2>/dev/null || echo \"\")\n  echo \"attempt ${i}: sync=${SYNC} health=${HEALTH}\
        \ operation=${PHASE:-none}\"\n\n  # A sync operation that actually errored\
        \ is a real failure.\n  if [ \"${PHASE}\" = \"Failed\" ] || [ \"${PHASE}\"\
        \ = \"Error\" ]; then\n    echo \"ArgoCD sync operation reported ${PHASE}.\"\
        \n    kubectl -n argocd get application springboot-app \\\n      -o jsonpath='{.status.operationState.message}'\
        \ || true\n    echo \"\"\n    exit 1\n  fi\n\n  case \"${HEALTH}\" in\n  \
        \  Healthy)\n      echo \"ArgoCD reports the application Healthy (sync=${SYNC}).\"\
        \n      exit 0\n      ;;\n    Suspended)\n      echo \"Rollout is paused awaiting\
        \ canary analysis or manual promotion.\"\n      echo \"The application is\
        \ reconciled; treating as success.\"\n      exit 0\n      ;;\n    Degraded)\n\
        \      echo \"ArgoCD reports the application Degraded. Diagnostics follow:\"\
        \n      kubectl -n springboot-app get pods -o wide || true\n      kubectl\
        \ -n springboot-app describe rollout springboot-app || true\n      exit 1\n\
        \      ;;\n  esac\n  sleep 15\ndone\necho \"ArgoCD did not reach a healthy\
        \ state in time. Diagnostics follow:\"\nkubectl -n argocd get application\
        \ springboot-app -o yaml || true\nkubectl -n springboot-app get pods -o wide\
        \ || true\nkubectl -n springboot-app get events --sort-by=.lastTimestamp ||\
        \ true\nexit 1\n"
      timeout-minutes: 12
    - name: Report reconciliation detail
      run: "echo \"--- application resources ---\"\nkubectl -n argocd get application\
        \ springboot-app \\\n  -o jsonpath='{range .status.resources[*]}{.kind}/{.name}\
        \ {.status}{\"\\n\"}{end}' || true\necho \"--- rollout ---\"\nkubectl -n springboot-app\
        \ get rollout springboot-app -o wide || true\n"
    - name: Probe the application through the load balancer
      run: "set -euo pipefail\nfor i in $(seq 1 45); do\n  HOST=$(kubectl -n springboot-app\
        \ get ingress springboot-app \\\n    -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'\
        \ 2>/dev/null || echo \"\")\n  if [ -n \"${HOST}\" ]; then\n    if curl --fail\
        \ --silent --show-error --max-time 10 \"http://${HOST}/actuator/health\";\
        \ then\n      echo \"\"\n      echo \"Application healthy at http://${HOST}\"\
        \n      {\n        echo \"### Application is live\"\n        echo \"\"\n \
        \       echo \"http://${HOST}\"\n      } >> \"$GITHUB_STEP_SUMMARY\"\n   \
        \   exit 0\n    fi\n  fi\n  echo \"attempt ${i}: load balancer not serving\
        \ yet\"\n  sleep 20\ndone\necho \"Application never became reachable. Diagnostics\
        \ follow:\"\nkubectl -n springboot-app describe ingress springboot-app ||\
        \ true\nkubectl -n springboot-app get pods -o wide || true\nexit 1\n"
      timeout-minutes: 12
    - name: Report the ArgoCD console address
      run: "ARGOCD_HOST=\"\"\nfor i in $(seq 1 20); do\n  ARGOCD_HOST=$(kubectl -n\
        \ argocd get ingress argocd-server \\\n    -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'\
        \ 2>/dev/null || echo \"\")\n  if [ -n \"${ARGOCD_HOST}\" ]; then\n    break\n\
        \  fi\n  echo \"attempt ${i}: ArgoCD ingress address not assigned yet\"\n\
        \  sleep 15\ndone\n{\n  echo \"### ArgoCD console \u2014 PUBLIC, HTTP, admin-credentialed\"\
        \n  echo \"\"\n  if [ -n \"${ARGOCD_HOST}\" ]; then\n    echo \"http://${ARGOCD_HOST}\"\
        \n    echo \"\"\n    if curl --fail --silent --show-error --max-time 10 \\\
        \n         \"http://${ARGOCD_HOST}/healthz\" >/dev/null 2>&1; then\n     \
        \ echo \"Health check: serving.\"\n    else\n      echo \"Health check: not\
        \ answering yet (ALB target registration\"\n      echo \"takes a few minutes\
        \ after the ingress is created).\"\n    fi\n  else\n    echo \"Ingress address\
        \ not assigned yet.\"\n  fi\n  echo \"\"\n  echo \"Reachable from 0.0.0.0/0\
        \ with no TLS and no SSO. Demo only.\"\n} >> \"$GITHUB_STEP_SUMMARY\"\n"
      timeout-minutes: 8
