# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Compile and verify formatting
      run: mvn -B -ntp clean compile
      working-directory: app
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Run unit tests
      run: mvn -B -ntp test
      working-directory: app
    - name: Publish test summary
      run: 'echo "### Test results" >> "$GITHUB_STEP_SUMMARY"

        find app/target/surefire-reports -name ''*.txt'' -exec cat {} + >> "$GITHUB_STEP_SUMMARY"
        || true

        '
  security:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: OWASP dependency vulnerability scan
      run: mvn -B -ntp org.owasp:dependency-check-maven:12.1.0:check -DfailBuildOnCVSS=8
        -DnvdApiKey="${{ secrets.NVD_API_KEY }}"
      working-directory: app
    - name: Upload dependency-check report
      uses: actions/upload-artifact@v4
      with:
        name: dependency-check-report
        path: app/target/dependency-check-report.html
  build_push:
    runs-on: ubuntu-latest
    needs:
    - test
    - security
    env:
      AWS_REGION: us-east-1
    timeout-minutes: 30
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Package application
      run: mvn -B -ntp clean package -DskipTests
      working-directory: app
    - id: meta
      name: Derive image tag
      run: echo "image_tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"
    - name: Build image
      run: docker build -t local/app:${{ steps.meta.outputs.image_tag }} .
      working-directory: app
    - name: Scan image with Trivy
      uses: aquasecurity/trivy-action@0.28.0
      with:
        image-ref: local/app:${{ steps.meta.outputs.image_tag }}
        format: table
        exit-code: '1'
        severity: HIGH,CRITICAL
        ignore-unfixed: 'true'
    - name: Authenticate to AWS via OIDC
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ secrets.AWS_CI_ROLE_ARN }}
        aws-region: us-east-1
    - id: ecr
      name: Log in to ECR
      uses: aws-actions/amazon-ecr-login@v2
    - name: Tag and push image
      run: 'set -euo pipefail

        TAG="${{ steps.meta.outputs.image_tag }}"

        docker tag "local/app:${TAG}" "${REGISTRY}/${{ secrets.PROJECT_NAME }}:${TAG}"

        docker push "${REGISTRY}/${{ secrets.PROJECT_NAME }}:${TAG}"

        '
      env:
        REGISTRY: ${{ steps.ecr.outputs.registry }}
  provision:
    runs-on: ubuntu-latest
    needs:
    - build_push
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 45
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Terraform validate
      run: terraform validate
      working-directory: infra
    - name: Terraform apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 45
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.33.0
    - name: Set up Helm
      uses: azure/setup-helm@v4
      with:
        version: v3.16.3
    - id: tf
      name: Read infrastructure outputs from terraform state
      run: "set -euo pipefail\nterraform init -input=false -reconfigure \\\n  -backend-config=\"\
        bucket=${{ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"cluster_name=$(terraform output -raw cluster_name)\" >> \"$GITHUB_OUTPUT\"\
        \necho \"lbc_role_arn=$(terraform output -raw lbc_role_arn)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Update kubeconfig
      run: aws eks update-kubeconfig --name "${{ steps.tf.outputs.cluster_name }}"
        --region us-east-1
    - name: Install AWS Load Balancer Controller
      run: "set -euo pipefail\nhelm repo add eks https://aws.github.io/eks-charts\n\
        helm repo update\nkubectl create serviceaccount aws-load-balancer-controller\
        \ -n kube-system \\\n  --dry-run=client -o yaml | kubectl apply -f -\nkubectl\
        \ annotate serviceaccount aws-load-balancer-controller -n kube-system \\\n\
        \  eks.amazonaws.com/role-arn=\"${{ steps.tf.outputs.lbc_role_arn }}\" --overwrite\n\
        helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller\
        \ \\\n  --version 1.10.1 --namespace kube-system \\\n  --set clusterName=\"\
        ${{ steps.tf.outputs.cluster_name }}\" \\\n  --set serviceAccount.create=false\
        \ \\\n  --set serviceAccount.name=aws-load-balancer-controller \\\n  --set\
        \ region=us-east-1 \\\n  --wait --timeout 10m\n"
    - name: Install Argo Rollouts controller
      run: "set -euo pipefail\nhelm repo add argo https://argoproj.github.io/argo-helm\n\
        helm repo update\nhelm upgrade --install argo-rollouts argo/argo-rollouts\
        \ \\\n  --version 2.38.2 --namespace argo-rollouts --create-namespace \\\n\
        \  --wait --timeout 10m\n"
    - name: Install kube-prometheus-stack
      run: "set -euo pipefail\nhelm repo add prometheus-community https://prometheus-community.github.io/helm-charts\n\
        helm repo update\nhelm upgrade --install monitoring prometheus-community/kube-prometheus-stack\
        \ \\\n  --version 66.3.1 --namespace monitoring --create-namespace \\\n  --values\
        \ gitops/values/monitoring-values.yaml \\\n  --set grafana.adminPassword=\"\
        ${{ secrets.GRAFANA_ADMIN_PASSWORD }}\" \\\n  --wait --timeout 15m\n"
    - name: Install ArgoCD
      run: "set -euo pipefail\nhelm upgrade --install argocd argo/argo-cd \\\n  --version\
        \ 7.7.11 --namespace argocd --create-namespace \\\n  --values gitops/values/argocd-values.yaml\
        \ \\\n  --wait --timeout 15m\n"
    - name: Register application repository with ArgoCD
      run: "set -euo pipefail\nkubectl -n argocd create secret generic repo-monorepo\
        \ \\\n  --from-literal=type=git \\\n  --from-literal=url=\"https://github.com/${{\
        \ github.repository }}.git\" \\\n  --from-literal=username=git \\\n  --from-literal=password=\"\
        ${{ secrets.ARGOCD_REPO_TOKEN }}\" \\\n  --dry-run=client -o yaml | kubectl\
        \ apply -f -\nkubectl -n argocd label secret repo-monorepo argocd.argoproj.io/secret-type=repository\
        \ --overwrite\n"
    - name: Seed the ECR repository URL into the GitOps values
      run: 'set -euo pipefail

        ECR_URL="$(terraform output -raw ecr_repository_url)"

        cd ..

        python3 - "$ECR_URL" <<''PY''

        import sys, pathlib

        ecr = sys.argv[1]

        p = pathlib.Path("gitops/application-values.yaml")

        text = p.read_text()

        text = text.replace("PLACEHOLDER_ECR_REPOSITORY", ecr)

        p.write_text(text)

        print(f"gitops/application-values.yaml now points at {ecr}")

        PY

        '
      working-directory: infra
    - name: Pin the freshly built image tag in the GitOps values
      run: 'set -euo pipefail

        TAG="${GITHUB_SHA::12}"

        python3 - "$TAG" <<''PY''

        import sys, pathlib, re

        tag = sys.argv[1]

        p = pathlib.Path("gitops/application-values.yaml")

        text = p.read_text()

        text = text.replace("PLACEHOLDER_IMAGE_TAG", tag)

        text = re.sub(r''(?m)^(\s*tag:\s*).*$'', lambda m: m.group(1) + tag, text,
        count=1)

        p.write_text(text)

        print(f"gitops/application-values.yaml pinned to image tag {tag}")

        PY

        '
    - name: Commit the GitOps values if they changed
      run: "set -euo pipefail\ngit config user.name \"github-actions[bot]\"\ngit config\
        \ user.email \"github-actions[bot]@users.noreply.github.com\"\nif git diff\
        \ --quiet -- gitops/application-values.yaml; then\n  echo \"GitOps values\
        \ already current; nothing to commit.\"\nelse\n  git add gitops/application-values.yaml\n\
        \  # [skip ci] is loop-prevention layer 1: this commit must never\n  # start\
        \ another delivery run.\n  git commit -m \"chore(gitops): pin image tag ${GITHUB_SHA::12}\
        \ [skip ci]\"\n  git push origin HEAD:main\nfi\n"
    - name: Apply the ArgoCD root application
      run: "set -euo pipefail\nsed \"s|__REPO_URL__|https://github.com/${{ github.repository\
        \ }}.git|g\" \\\n  gitops/root-app.yaml | kubectl apply -f -\n"
  verify:
    runs-on: ubuntu-latest
    needs:
    - provision
    - configure
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: us-east-1
      TF_VAR_github_repo: ${{ github.repository }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.9.8
    - name: Set up kubectl
      uses: azure/setup-kubectl@v4
      with:
        version: v1.33.0
    - id: tf
      name: Read cluster name from terraform state
      run: "set -euo pipefail\nterraform init -input=false -reconfigure \\\n  -backend-config=\"\
        bucket=${{ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\necho\
        \ \"cluster_name=$(terraform output -raw cluster_name)\" >> \"$GITHUB_OUTPUT\"\
        \n"
      working-directory: infra
    - name: Update kubeconfig
      run: aws eks update-kubeconfig --name "${{ steps.tf.outputs.cluster_name }}"
        --region us-east-1
    - name: Show cluster nodes
      run: kubectl get nodes -o wide
    - name: Wait for ArgoCD to sync the application
      run: "set -euo pipefail\nfor i in $(seq 1 60); do\n  SYNC=$(kubectl -n argocd\
        \ get application springboot-app -o jsonpath='{.status.sync.status}' 2>/dev/null\
        \ || echo Unknown)\n  HEALTH=$(kubectl -n argocd get application springboot-app\
        \ -o jsonpath='{.status.health.status}' 2>/dev/null || echo Unknown)\n  echo\
        \ \"attempt ${i}: sync=${SYNC} health=${HEALTH}\"\n  case \"${SYNC}/${HEALTH}\"\
        \ in\n    Synced/Healthy)\n      echo \"ArgoCD reports the application Synced\
        \ and Healthy.\"\n      exit 0\n      ;;\n    Synced/Suspended)\n      echo\
        \ \"Rollout is paused awaiting analysis or manual promotion; treating as reconciled.\"\
        \n      exit 0\n      ;;\n  esac\n  sleep 15\ndone\necho \"ArgoCD did not\
        \ reconcile in time. Diagnostics follow:\"\nkubectl -n argocd get application\
        \ springboot-app -o yaml || true\nkubectl -n springboot-app get pods -o wide\
        \ || true\nkubectl -n springboot-app get events --sort-by=.lastTimestamp ||\
        \ true\nexit 1\n"
      timeout-minutes: 12
    - name: Show rollout status
      run: kubectl -n springboot-app get rollout springboot-app -o wide || true
    - name: Probe the application through the load balancer
      run: "set -euo pipefail\nfor i in $(seq 1 45); do\n  HOST=$(kubectl -n springboot-app\
        \ get ingress springboot-app \\\n    -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'\
        \ 2>/dev/null || echo \"\")\n  if [ -n \"${HOST}\" ]; then\n    if curl --fail\
        \ --silent --show-error --max-time 10 \"http://${HOST}/actuator/health\";\
        \ then\n      echo \"\"\n      echo \"Application healthy at http://${HOST}\"\
        \n      echo \"APPLICATION_URL=http://${HOST}\" >> \"$GITHUB_STEP_SUMMARY\"\
        \n      exit 0\n    fi\n  fi\n  echo \"attempt ${i}: load balancer not serving\
        \ yet\"\n  sleep 20\ndone\necho \"Application never became reachable. Diagnostics\
        \ follow:\"\nkubectl -n springboot-app describe ingress springboot-app ||\
        \ true\nkubectl -n springboot-app get pods -o wide || true\nexit 1\n"
      timeout-minutes: 12
